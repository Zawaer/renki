# React Native's Metro bundler can't traverse pnpm's isolated symlink store to
# find transitive deps (e.g. @babel/runtime). A flat node_modules is the layout
# Metro expects — this is Expo's recommended setting for pnpm monorepos.
node-linker=hoisted
