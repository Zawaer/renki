server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Client-side routes (e.g. /stats) aren't real files — fall back to the SPA
  # shell so direct navigation and refreshes work, not just in-app nav.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
